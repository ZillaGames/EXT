using UnityEngine;
using UnityEngine.Events;
using UnityEngine.UI;

[RequireComponent(typeof(Button))]
public class OpenPopUpButton : MonoBehaviour
{
    /*
    [SerializeField] PopupData PopUp;
    [SerializeField] PopupEvents events;

    private void Awake()
        => GetComponent<Button>()
            .onClick.AddListener(
                () => events.OpenPopupEvent?.Invoke(PopUp));
    */
}
